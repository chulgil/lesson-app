# 수강권 스케줄 변경 UX 스펙

> 작성일: 2026-04-05
> 상태: ✅ 확정
> 최종 업데이트: 2026-04-14 (UI 일관성 패치 반영)
> 관련: subscription_schedule_management_spec.md, lesson_lifecycle_chapters.md
> UI 공통 스펙: `docs/specs/design/detail_screen_template.md`

---

## 1. 전체 구조 변경

### 1.1 레슨 요청 → 4단계 마무리

```
레슨 요청 라이프사이클 (변경 없음):
  신청 → 확정 → 결제 → 완료(수강권 발급)

수강권 발급 후 → 별도 "수강권 관리" 화면으로 전환
```

### 1.2 홈 화면 재구성

```
[변경 전]                          [변경 후]
홈:                                홈:
├── 레슨 요청 섹션                  ├── 레슨 요청 섹션 (유지)
├── 수강권 섹션 ← 제거              ├── 스케줄 변경요청 섹션 ← NEW
└── 과제 요약                      └── 과제 요약

수강권 리스트 → 학생 상세 화면의 "수강권 현황"으로 이동
```

### 1.3 네비게이션 플로우

```
선생님 홈
├── 레슨 요청 (N) ─── 탭 → 레슨 요청 상세 (기존)
│
├── 스케줄 변경요청 (N) ─── 탭 → 스케줄 변경 상세 (NEW)
│   └── [전체보기] → 스케줄 변경요청 전체 목록
│
└── 학생 탭 → 학생 상세
    └── 수강권 현황
        └── 수강권 카드 탭 → 스케줄 변경 상세 (동일 화면)
```

---

## 2. 스케줄 변경 상세 화면 (핵심)

### 2.1 전체 레이아웃

> **레슨요청 상세 화면(`request_detail_screen.dart`)과 동일한 UI 패턴 적용**
> 상세 공통 스펙: `docs/specs/design/detail_screen_template.md`

```
┌──────────────────────────────────────┐
│  ← 서울음악학원 김소연 (8회권)       │ AppBar
│                                      │ titleSpacing: 0
│                                      │ headingSmall
├──────────────────────────────────────┤
│                                      │
│     ●───●───●───◉───○───○───○───○  │ 회차 프로그레스 바
│    1회  2회  3회  4회  5회  6회  7회  8회│ (Expanded 커넥터, 가운데 정렬)
│                                      │
│  ℹ️ 회차를 터치하여 일정을 변경할    │ 가이드 인포박스
│     수 있습니다.                     │
│                                      │
├──────────────────────────────────────┤
│                                      │ 챗 영역 (스크롤)
│  ── 1회차 · 4/5(토) 14:00 완료 ──  │ 접힌 회차 (과거)
│  ── 2회차 · 4/12(토) 14:00 완료 ── │ 접힌 회차 (과거)
│  ── 3회차 · 4/19(토) 14:00 완료 ── │ 접힌 회차 (과거)
│                                      │
│  ══ 4회차 · 4/26(토) 14:00 예정 ══ │ 펼쳐진 회차 (현재 선택)
│                                      │
│  [학생 버블]                         │ 변경 요청 챗
│  "4회차 시간 변경 부탁드립니다"      │
│  변경취소권 1회 사용 (잔여 1회)      │
│                                      │
│  [선생님 버블]                       │
│  "가능한 시간 3개를 보내드려요"      │
│  ┌────────────────────────┐        │
│  │ 1순위 일 10:00~11:00   │        │ TimeSlotOption 카드
│  │ 2순위 월 14:00~15:00   │        │
│  │ 3순위 화 10:00~11:00   │        │
│  └────────────────────────┘        │
│                                      │
├──────────────────────────────────────┤
│  [학생에게 전달할 메시지를 입력하세요]│ 하단 입력 바
│  ┌─────────────┐ ┌──────────────┐  │
│  │  일정 변경   │ │  메시지 전송  │  │ 2-버튼 Row
│  └─────────────┘ └──────────────┘  │ (Outlined + Filled)
└──────────────────────────────────────┘
```

### 2.2 AppBar 상세

> **레슨요청 상세와 동일한 포맷**

| 항목 | 규칙 |
|------|------|
| titleSpacing | 0 |
| 포맷 | `학원이름 학생이름 (타입)` 또는 `학생이름 (타입)` |
| 학원 판별 | `LessonClass.type == academy` → `LessonClass.name`이 학원이름 |
| 타입 라벨 | `subscription.typeLabel` (체험/8회권/월정액 등) |
| 스타일 | `AppTypography.headingSmall` |

### 2.3 회차 프로그레스 바 상세

> **LessonProgressBar와 pixel-exact 동일 패턴**

#### 배치 규칙
- **8개/줄** — 8회차 이하: 1줄, 9~16: 2줄, 17~24: 3줄
- 각 회차는 원형 인디케이터 (dot 20px, ring 28px)
- **[전체]** 버튼: 프로그레스 바 우측 끝 (월정액만)
- **커넥터**: `Expanded` — 완료=실선(primary), 미완료=점선(borderLight)
- **패딩**: screenPadding (h), space2 (v) — 위젯 내부에서 적용

#### 상태별 표시

| 상태 | 시각 | 색상 | 라벨 |
|------|------|------|------|
| 완료 | filled circle + ✓ checkmark (12px) | **primary** | primary, normal |
| 다음 예정 (선택) | filled circle + outer ring (width 2) | **primary** + primaryLight ring | primary, w600 |
| 미래 | hollow circle (border 1.5px) | borderLight | textTertiaryLight, normal |
| 변경 요청 중 | warning dot (top-right 오버레이) | warning | — |

#### 자동 선택 규칙
- 화면 진입 시, **현재 일자 기준 다음에 진행할 회차**가 자동 선택
- 자동 선택된 회차의 챗 영역이 펼쳐지고 스크롤 위치 조정

#### [전체] 버튼 동작
```
[전체] 탭 시:
1. 현재~마지막 미완료 회차 전체 선택 (하이라이트)
2. 하단 버튼 라벨: "일정 변경" → "전체 스케줄 변경"
3. 일정 제안 시 말풍선: "전체 회차(4~10) 일정 변경을 제안했습니다"
4. 정규권에서만 활성화 (회차권은 비활성)
```

### 2.4 챗 영역 상세

#### 표시 내용
- **스케줄 변경 이벤트만 표시** (수강권 발급 카드 X)
- 각 회차별 스케줄 변경 요청/응답/확정 이벤트

#### 회차별 접기/펼치기

```
접힌 상태 (과거 완료):
  ── 1회차 · 4/5(토) 14:00 완료 ──     ← 한 줄 요약, 탭하면 펼침

펼쳐진 상태:
  ══ 4회차 · 4/26(토) 14:00 예정 ══    ← 강조 헤더
  │                                     │
  │  [학생 버블]                        │  스케줄 변경 챗 이벤트들
  │  "시간 변경 요청..."               │
  │                                     │
  │  [선생님 버블]                      │
  │  "가능한 시간..."                  │
  │                                     │
  ══════════════════════════════════     ← 회차 끝 구분선
```

#### 프로그레스 바 ↔ 챗 영역 연동
- 프로그레스 바에서 회차 탭 → 해당 회차 **펼침 + 자동 스크롤**
- 펼쳐진 회차가 있으면 기존 펼침 닫힘 (아코디언 방식)
- 과거 완료 회차도 탭하면 펼침 가능 (읽기 전용, 변경 불가)

### 2.5 하단 입력 바

> **CurrentRequestBox와 동일한 패턴**
> 상세: `docs/specs/design/detail_screen_template.md` 참조

```
┌──────────────────────────────────────┐
│  [학생에게 전달할 메시지를 입력하세요]│ TextField (bodySmall, radiusMedium)
│                                      │ maxLines: 8, minLines: 1, maxLength: 200
│  ┌─────────────┐ ┌──────────────┐  │
│  │  일정 변경   │ │  메시지 전송  │  │ Row: Outlined + Filled
│  └─────────────┘ └──────────────┘  │ buttonHeightSmall, radiusMedium
└──────────────────────────────────────┘
```

| 항목 | 값 |
|------|-----|
| 컨테이너 | surfaceLight + borderLight top + space3 패딩 + SafeArea |
| 메시지 힌트 | "학생에게 전달할 메시지를 입력하세요" |
| 보조 버튼 (좌) | "일정 변경" — OutlinedButton, textSecondaryLight |
| 주요 버튼 (우) | "메시지 전송" — ElevatedButton, primary/white |
| 버튼 높이 | `AppSpacing.buttonHeightSmall` (40) |
| 버튼 폰트 | `AppTypography.buttonSmall` |
| 버튼 radius | `AppSpacing.radiusMedium` |

### 2.6 일정 변경 말풍선 구조

#### 학생 → 선생님 (변경 요청)
```
[학생 버블]
"4회차 시간 변경을 요청합니다"
변경취소권 1회 사용 (잔여 1회)
───────────
사유: 개인 일정 변경
```

#### 선생님 → 학생 (시간 제안)
```
[선생님 버블]
"4회차 일정 변경을 제안했습니다"
┌────────────────────────┐
│ 1순위 일 10:00~11:00   │
│ 2순위 월 14:00~15:00   │
│ 3순위 화 10:00~11:00   │
└────────────────────────┘
메시지: "일요일 오전이 가장 여유있습니다"
```

#### 전체 회차 변경 시
```
[선생님 버블]
"전체 회차(4~10) 일정 변경을 제안했습니다"
┌────────────────────────┐
│ 매주 일 10:00~11:00    │  ← 고정 스케줄 1개
└────────────────────────┘
```

#### 학생 수락
```
[학생 버블]
"1순위 일 10:00~11:00을 선택했습니다"

✅ 4회차 → 4/27(일) 10:00 확정
```

---

## 3. 홈 화면 — 스케줄 변경요청 섹션

### 3.1 섹션 구조

```
┌──────────────────────────────────────┐
│  📅 스케줄 변경요청 (3)   [전체보기]  │ 헤더
│  변경 2 · 취소 1                     │ 상태 요약
│                                      │
│  ┌────────────────────────────────┐ │ 변경요청 미니 카드
│  │ 🔵 김소연 4회차 시간변경 · 2시간│ │
│  │ 🔴 이민수 5회차 취소   · 30분  │ │
│  │ 🔵 박지현 3회차 시간변경 · 1일 │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

### 3.2 미니 카드 탭 동작
- 탭 → 스케줄 변경 상세 화면 진입 (해당 회차 자동 선택)

### 3.3 전체보기 화면

```
┌──────────────────────────────────────┐
│  ← 스케줄 변경요청                    │ AppBar
├──────────────────────────────────────┤
│  [대기중(3)] [완료(5)] [전체(8)]     │ 필터 탭
├──────────────────────────────────────┤
│  김소연 · 바이올린 · 4회차 시간변경   │ 리스트 아이템
│  2시간 전 · 🔵 대기중                │
│  ─────────────────────────────────  │
│  이민수 · 피아노 · 5회차 취소        │
│  30분 전 · 🔴 대기중                │
└──────────────────────────────────────┘
```

---

## 4. 정규권 vs 회차권 차이

| | 정규권 (monthly) | 회차권 (package) |
|---|---|---|
| 프로그레스 바 | 전체 회차 표시 (예정 시간 포함) | 완료 + 예약 필요 + 미정 |
| [전체] 버튼 | ✅ 활성화 | ❌ 비활성 (회색) |
| 미래 회차 | 자동 생성된 시간 표시 | "예약 필요" 표시 |
| 변경 말풍선 | "4회차" 또는 "전체 회차(4~10)" | "3회차" (개별만) |
| 빈 회차 (예약 필요) | 해당 없음 | 학생이 빈 시간대 선택 UI |

---

## 5. 가이드 메시지 체계

### 5.1 인포박스 (프로그레스 바 하단, 항상 표시)

| 상황 | 메시지 |
|------|--------|
| 기본 | "회차를 터치하여 일정을 변경할 수 있습니다" |
| 전체 모드 선택 시 | "앞으로의 전체 스케줄을 변경합니다" |
| 변경취소권 0회 | "변경취소권이 소진되어 일정 변경이 불가합니다" |

### 5.2 챗 내 가이드 (첫 진입 시 1회)

| 시점 | 메시지 |
|------|--------|
| 정규권 첫 진입 | "고정 스케줄이 자동 생성되었습니다. 회차를 터치하여 변경하세요." |
| 회차권 1회 완료 후 | "다음 레슨 시간을 선생님의 빈 시간대에서 선택하세요." |

---

## 6. 기존 화면 수정 사항

| 화면 | 변경 |
|------|------|
| `dashboard_tab.dart` | TeacherSubscriptionSection → ScheduleChangeRequestSection 교체 |
| `subscription_detail_screen.dart` | 전면 리디자인 → 회차 프로그레스 + 챗 (request_detail과 동일 패턴) |
| `subscription_chapter_lessons.dart` | 삭제 또는 새 위젯으로 대체 |
| `student_subscription_section.dart` | 수강권 카드 리스트 유지 (홈에서 이동) |
| `subscription_list_screen.dart` | 학생 상세에서만 접근 (홈 진입점 제거) |
| `teacher_subscription_list_screen.dart` | → ScheduleChangeRequestListScreen 으로 용도 변경 |
| `subscription_bottom_input_bar.dart` | 메시지 전송 + 일정 변경 버튼 (CurrentRequestBox 패턴) |
| `session_progress_bar.dart` | LessonProgressBar와 pixel-exact 동일 (Expanded 커넥터, primary 색상) |
| `reschedule_bottom_sheet.dart` | 유지 (일정 변경 버튼에서 호출) |
| `cancel_lesson_bottom_sheet.dart` | 유지 |

---

## 7. 데이터 플로우

```
[이벤트 저장]
  학생/선생님 액션 → RequestEvent 생성
    - subscriptionId
    - sessionNumber (또는 null = 전체)
    - eventType (scheduleChangeRequested / Proposed / Accepted / Countered)
    - suggestedSlots (시간 제안 시)
    - message (텍스트 메시지)

[화면 로딩]
  subscriptionSessionEventsProvider(subscriptionId) → 전체 이벤트
  → sessionNumber별 그룹핑 → 회차별 챗 렌더링
```

---

## 8. 구현 Phase (확정)

| Phase | 내용 | 상태 |
|-------|------|------|
| D-1 | 홈 화면 수강권 섹션 → 스케줄 변경요청 섹션 교체 | ✅ |
| D-2 | 회차 프로그레스 바 위젯 (LessonProgressBar 패턴 통일) | ✅ |
| D-3 | 수강권 상세 리디자인 (프로그레스 + 챗 + 하단 입력) | ✅ |
| D-4 | ~~수강권 발급 카드 말풍선~~ → 제거 (스케줄 변경만 표시) | ✅ |
| D-5 | 일정 변경 챗 이벤트 (말풍선 구조 + 슬롯 카드) | |
| D-6 | 스케줄 변경요청 전체보기 화면 | ✅ |
| D-7 | 가이드 메시지 + 인포박스 | ✅ |

---

## 변경 이력

| 날짜 | 변경 |
|------|------|
| 2026-04-05 | 초안 작성 |
| 2026-04-14 | UI 일관성 패치: request_detail과 pixel-exact 통일. 프로그레스바 색상(success→primary), 커넥터(Expanded), AppBar(학원+학생+타입), 하단바(메시지전송+일정변경), 챗(수강권 발급 카드 제거) |
