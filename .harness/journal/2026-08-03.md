# Journal — 2026-08-03

feature: lesson-add-intent (Phase 5, PR-A BE)
worktree: `.claude/worktrees/lesson-add-intent-be` (origin/main d4e8e9b7 기반)

## 12:10 — J1: BE overflow_mode + is_preview 컬럼
- 결정: overflow 처리를 파라미터 1개로 수렴 (S3 시트·S4 토글 공용 계약). preview 는 LessonStatus enum 확장 대신 boolean 컬럼.
- 산출물: `9b003156` — schemas/models/service + 마이그레이션 `lesson_is_preview` + 테스트 9
- 비고: BE Lesson 에 is_preview 부재 발견 (FE lesson.g.dart 는 이미 파싱) — 계획의 "기존 필드 재사용" 전제가 FE-only 였음. additive 마이그레이션 1개로 계약 완성.

## 12:55 — J2: makeup_credit 소비 모드
- 결정: 크레딧 레슨 식별 = MakeupCredit.used_lesson_id 연결 (신규 컬럼 0). scheduled_lessons 증가는 booking 경로 정합 위해 보류.
- 산출물: `4b067eba` — 소비 + 완료 차감 스킵 + 테스트 4. use_credit naive/aware datetime 잠재 버그 수정(_ensure_aware).
- 비고: **드리프트 관찰** — booking 경로가 §5.3 scheduled_lessons+=1 미수행. 후속 이슈 후보.

## 13:20 — J3: 체험권 재생성 가드
- 결정: FE 트리거 = detail 안정 키 "trial_already_used". 미가입 학생 비적용 (D4 정합).
- 산출물: `755b9743` — _assert_trial_regeneration_allowed + 테스트 2

## 13:40 — J4: 미가입 즉시발급 카드/알림 스킵 (조사)
- 결정: 무변경 종결 — 카드는 제안 입금확인 경로 전용, 알림은 user_id 게이트로 이미 스킵. 회귀 테스트로 고정.
- 산출물: `6dd77f76` — test_manual_student_direct_issue 1

## 14:15 — J16: preview 정식 전환
- 결정: 학생 단위 전량 승격 (발급 경로 무관 동일 동작). 취소된 preview 는 is_preview 마커 유지. 명시 FK 거절.
- 산출물: `9a5b33e1` — promote/cancel 헬퍼 + 발급 2경로·거절/취소/만료 4지점 배선 + 테스트 3
- 비고: autoflush off-by-one (재귀속 후 번호 산출 시 자기 자신 카운트) — 번호 선산출로 해결.

## 14:25 — PR-A 마감
- BE 전체 스위트 1522 passed / 실패 2 = stash baseline 확인된 main 기존 휴가 날짜의존 (회귀 0).
- **PR #1227** OPEN — 머지 결정 = 사용자. 드리프트 게이트: 코드 전용(harness diff 0) 확인 후 --no-verify.
- 다음: PR-B (FE 인텐트 J5~J7 — J1/J2/J3 계약 소비), PR-C (J8~J9), PR-D (J10~J11). 새 세션 + FE worktree 권장.
- [14:53] task done: Figma 전면 검수: 앱↔Figma 인벤토리 대조
- [15:00] task done: 신규 UI 커밋 미반영분 Figma 반영
- [15:03] task done: 라운드트립 가능성 진단 + 최종 피드백 문서

## 15:55 — PR-A 머지 + PR-B(J5~J7) 완료
- PR #1227 머지(`83d3403a`) — PG CI 실패 2건 = main 동일 실패 교차 확인 후 머지. BE worktree 정리.
- J5 `d79c6c6c`: S5 발급 버튼 + S6 trial_already_used 다이얼로그.
- J6 `359e4cc3`: 잔여 0 시트 + overflowMode 와이어 4파일 + shouldPromptOverflowMode 술어(S1/S2 회귀 고정). 결정: 기록 모드 갱신 숨김(보너스 기본), 정기 반복은 레거시 유지, 갱신 이동=pushReplacement.
- J7 `ed2f0a68`: 크레딧 토글(기본 OFF) + 보강 배지(usedLessonId 연결). 체험 배지=기존 SubscriptionBadge 확인.
- 검증: 신규 21 테스트 + lessons 113 + architecture 78 + 전체 3540(-2 = main 기존 per_student_disposition). **PR #1228 OPEN** — 머지 결정 = 사용자.
- 비고: 워크트리 PostToolUse i18n 훅이 stderr 없이 blocking 오탐 (수동 실행 exit 0 확인) — 하네스 경로 문제, 후속 조사 후보.
- [16:20] task done: 학생 목록 탭 Figma 프레임을 매스트헤드 구조로 재작성
- [16:37] task done: 하단 네비 크기 변형 토큰을 코드에 정의 후 Figma 반영
- [17:40] task done: PR #1228 머지 + worktree 갱신
- [17:55] task done: PR-C 구현 (J8 returnTo 발급 왕복 + J9 새 학생 인라인 등록)
- [18:19] task done: PR-D 구현 (J10 다음 회차 CTA + J11 미가입 편집)

## 20:05 — PR-C/PR-D/후속 2건 + J12~J15
- PR-C `#1229` 머지(`35e4b0c9`): J8 returnTo 왕복 + 미가입 제안 숨김(AC-5.1) + J9 새 학생 인라인 등록.
- PR-D `#1230` 머지(`21c6abfe`): J10 CTA 배선(死문자열 부활) + J11 미가입 단독 편집 — **필드 잠금과 #766 plain-cancel 가드 분리** (미가입 해제가 회계 우회를 재개방하지 않도록). 회귀 1건 자체 발견(studentsProvider watch 타이머 누출)·수정.
- J12: FE 3555(-2 기존 per_student) + BE 1522(-2 기존 휴가) + arch 78 — 신규 실패 0.
- J13 code critic PASS (1차 파견 무응답 → 범위 좁혀 재파견). 위험 관찰 중 "preview 조기 완료 오차감" **실증**(Red: used 2==1) → `#1232` 머지(`e9a8094a`): is_preview 차감 가드.
- J14 test critic FAIL→수정(`#1231` 머지 `47f41f69`: D1 단언·알림 스킵 실검증·배선 단언)→**rev2 PASS**.
- J15 실기(mock 웹 375/1440): 커버 = 새학생 시트·S1/S2·S4 비노출·CTA 왕복 프리필·콘솔 0. 잔여 = S3/S4 ON/S5/S6/미가입 편집/제안 미선생성 — mock 상태 부재, 베타 실기 or mock 시나리오 보강 후. AC-6.3 in_progress.
- 후속 백로그: ①보너스 remaining +2 인플레이션(total+bonus 동시 증가, 레거시) ②booking scheduled_lessons 미증가 드리프트 ③repo→HTTP overflow_mode 직렬화 테스트 ④미가입 다중 템플릿 .first 무통보.
