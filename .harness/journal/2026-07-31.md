# Journal — 2026-07-31 (group-lesson Phase 5)

## 14:30 — Phase 5 시작
- 결정: worktree `feat/group-lesson`(base `ba5d3af1`, chore/cg-harness-resync)에서 실행. J2 는 alembic 헤드 충돌 방지 위해 J1 완료 후 순차.
- 산출물: worktree 생성, Wave 1 병렬 파견 — J1(BE GroupClass 배선 정합) + J8(FE 이모지 정리)
- 비고: steer inbox 비어 있음. J5a←J10 머지 제약은 DAG 로 강제됨.

## 14:35 — J8: FE 그룹 화면 이모지 정리 완료
- 결정: 악기 구분은 색상 담당(InstrumentColors) — 신규 악기→아이콘 매핑 유틸 발명 안 함 (기존 컨벤션 재사용)
- 산출물: commit `0e3a60c0` (2 files +37/-23). rg 이모지 게이트 exit 1, analyze 0, 기존 출석화면 테스트 2/2
- 비고: main 이 커밋·게이트 독립 재검증 완료. AC-7 passed.

## 15:10 — J1: BE GroupClass 배선 정합 완료
- 결정: 마이그레이션은 삭제 아닌 백필(mirror) — 예약·출석 이력 보존. 정원 SSOT 가 GroupClass 로 이동.
- 산출물: commit `30efa446` (9 files +519/-49). wiring 테스트 10/10(main 재실행 검증), 전체 pytest 1306 passed / 1 failed(선행 red) / 18 skipped. PG throwaway(:55432) upgrade head + 백필 실검증.
- 비고: (1) `test_alembic_sqlite_upgrade` 는 **선행 red** — base(ba5d3af1)에서도 실패 재현 확인(revert-oracle). 원인=2026-06-19 `min_booking_hours_default_zero` 의 SQLite 미지원 ALTER, J16 전에 별도 처리 결정 필요. (2) J3 전달: 반복 스케줄 생성 시 `max_capacity` 명시 금지 — GroupClass 상속에 맡길 것. (3) PG 게이트가 실버그 1건 검출(enum text 리터럴 INSERT) — PG 검증 절차 유효성 입증.

## 15:15 — Wave 2 파견: J2(BE appliesTo+groupClassId) ∥ J7(FE NoShowPolicy 4값)
- 결정: J3·J6·J10 은 전부 schedule_ext_service.py 접촉 → 상호 순차로 편성 (J2 완료 후 J3 부터). J7 은 FE 전용이라 J2 와 병렬 안전.
- 비고: J2 에 PG throwaway(:55432) 방식·camelCase enum values_callable 함정·J1 head 체인 전달함.

## 15:30 — J7: FE NoShowPolicy 4값 정합 완료
- 결정: bookingPolicyText 시그니처 bool→String (bool 이 4값 표현 불가). 출석 차감 루프는 불변 유지 — FE 분기 시 J5b BE 집행과 이중 차감 위험.
- 산출물: commit `38dae244` (7 files). analyze 0, no_show_policy 7/7(main 재검증), schedule 473/473. Red-Green 오라클 수행됨.
- 비고: AC-10 passed (BE 실집행은 AC-5/J5b). **선행 실패 원장**: ① BE `test_alembic_sqlite_upgrade` ② FE architecture 4건(73+4, base ba5d3af1 재현 — main 이 직접 재확인) — 둘 다 group-lesson 무관, J16 게이트 전 별도 처리 결정 필요.

## 15:32 — Wave 2.5 파견: J3(클래스 CRUD+반복 스케줄) ∥ J2(진행 중)
- 결정: J3 는 마이그레이션 불필요(테이블 기존)라 J2 와 alembic 충돌 없음 — 병렬 승격. J1 전달사항(정원은 GroupClass 상속, max_capacity 명시 금지) 브리프에 포함.

## 15:45 — J2: BE appliesTo+groupClassId 완료
- 결정: SubscriptionTemplate 에도 group_class_id 추가 (템플릿→발급 전파 계약) — 스펙 §4 역반영함. NULL=universal 해석은 `effective_applies_to` 프로퍼티 SSOT.
- 산출물: commit `77bc769d` (5 files +370). pytest 1308/1(선행 red)/18, 신규 2 테스트 main 재실행 2/2. PG throwaway 비파괴·멱등·왕복 검증 + enum camelCase 라벨 실증. revert-oracle 수행.
- 비고: J13 wire 계약 확정 — snake_case `applies_to`(문자열 "group")·`group_class_id`. J5a 진입 가능 조건 = J10 완료.

## 16:00 — J3: BE 클래스 CRUD + 반복 스케줄 완료
- 결정: prefix `/groups/classes`(기존 FE `/groups/*` 승계, 스펙 §4 역반영) · 반복 horizon 4주(#301 컨벤션 승계, rolling) · 회차 생성은 create_group_schedule 경유(정원 GroupClass 상속).
- 산출물: commit `e0371e91` (4 files +810). 신규 15/15(main 재실행), 전체 1323/1(선행 red)/18. revert-oracle 수행.
- 비고: 비활성화=is_active flag 만 — 미래 회차 일괄 취소는 범위 밖(J5/J10 주의사항으로 전달). J10 파견(크리티컬 패스), J4 는 J10 뒤 순차(같은 서비스 파일). J9a/J9b 는 J13 의 build_runner 종료 후(동시 codegen 충돌 방지).

## 16:30 — J13: FE 수강권 표시 정합 완료
- 결정: 표시 규칙 2분기는 presentation extension `subscription_scope_visuals.dart` 단일 SSOT. 그룹이면 멤버십 조회 자체를 스킵해 폴백 경로 원천 차단.
- 산출물: commit `2476b758` (15 files +606/-50). 신규 15 테스트(main 재실행 15/15), analyze 0, subscription+parent_home 295/0. 전체 FE 2865/4 — 실패 4건은 선행(HEAD 재현 확인, 원장 기존 항목과 동일).
- 비고: GroupClass 조회 provider 부재로 branch ① 실주입은 J12 에서 한 줄 연결 (미주입 시 branch ② 안전 폴백 — 개인레슨 불가). J9a/J9b 파견 (build_runner 단독 사용 가능 시점).

## 17:20 — J10: BE 그룹 알림 5종 완료 + 중대 발견 (stale base)
- 결정: 리마인더는 payment_reminder_jobs 규약 승계(APScheduler daily KST + advisory lock + *_sent_at 멱등). 전일 20:00/당일 08:00. 날짜 판정은 Python KST 정규화(SQLite tz 함정 실측).
- 산출물: commit `3b0ce9ab` (9 files +958). 신규 12/12(main 재실행), 전체 1335/1(선행 red)/18. PG 왕복 실증. notifications.type 은 VARCHAR — ADD VALUE 불요(실증 대체).
- **중대**: feat/group-lesson base 가 origin/main 대비 **201커밋 stale** (merge-base 3ec12e53, 스펙 기준 b8b1ddbc 미포함 — #1207 대기승급 알림 2종이 worktree 에 없음). main 실측 확인.
- **대응 결정**: j9-screens 완료 후 **origin/main 을 feat/group-lesson 에 merge** (rebase 금지 — journal 해시 보존, 병렬 에이전트 이력 보존). 머지 후 FE/BE 전체 스위트 재검증 → J5a 진행. 잔여 job 은 머지 후 base 에서.
- 후속: FE NotificationType 그룹 5종 미등록(폴백 렌더) → J12 브리프에 편입.

## 17:50 — J9a/J9b: FE 클래스 폼 + 내 클래스 목록 완료
- 결정: 목록→상세 연결은 onOpenClass 필수 콜백(회차 해석은 J12 몫). 드롭인은 생성 직후 단일 회차 POST /groups/schedules 동반(껍데기 클래스 방지). facade 로 teacherGroupClasses/groupClassById provider 좁게 export.
- 산출물: commits `332c8e4c`(+1900)·`aa1a03ea`(+479). 신규 12 테스트, analyze 0, schedule 485/485, arch baseline 불변. revert-oracle(invalidate) 수행.
- 비고: build_runner 잔여 무관 .g.dart 3건은 main 이 checkout 원복. AC-3 passed.

## 17:55 — base 머지 착수 (stale base 대응)
- 결정: harness 기록 커밋 후 origin/main 을 feat/group-lesson 에 merge --no-commit 로 진행 (머지 커밋도 lore 훅 트레일러 필요).
