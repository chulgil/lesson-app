# Constitution — 프로젝트 불변원칙 (MUST 게이트)

<!--
  이 파일은 cg-interview(Phase 1)가 생성·갱신하고, cg-plan-check(Phase 4.5)가
  게이트로 사용한다. 계획(DAG/job)이 아래 MUST 를 위반하면 기본 REVISE —
  위반이 불가피하면 계획 문서의 "## Complexity Tracking" 정당화가 있어야
  PASS-with-justification 으로 통과한다 (spec-kit 패턴).

  작성 규칙:
  - MUST 5~9개 청킹 유지 (Miller's Law, 7±2). 10개째가 필요하면 기존 항목과 병합.
  - 각 항목은 "검증 가능한 한 문장" + 근거. 모호한 선언("보안을 잘 지킨다") 금지.
  - 갱신은 append 가 아니라 개정 — 사람 확인 후 항목을 교체/병합한다.
  - 카테고리: 보안 / 아키텍처 / 데이터 / 범위 등 절대 제약만. feature 한정
    제약은 Phase 2 spec 의 제약 조건 섹션으로.
-->

| # | MUST 원칙 (검증 가능한 한 문장) | 근거 | 검증 방법 |
|---|--------------------------------|------|-----------|

<!-- 예시 (실제 작성 시 주석 해제 후 교체):
| 1 | 모든 외부 API 응답은 스키마 검증을 거친 뒤에만 도메인 계층에 진입한다 | 경계 검증 누락이 런타임 장애의 최다 원인 | 경계 모듈 단위 테스트 + 코드리뷰 체크 |
| 2 | 회원/결제 테이블 스키마 변경은 마이그레이션 스크립트 없이 금지 | 복구 불가능한 데이터 손실 방지 | PR diff 에 마이그레이션 파일 존재 확인 |
-->
